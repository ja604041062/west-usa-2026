# 使用說明

給你自己看的操作手冊。網站的正式網址是 **https://west-usa-2026.pages.dev/**，程式碼放在 **https://github.com/ja604041062/west-usa-2026**。

以下五件事涵蓋了你之後大概會想做的所有調整。每一件都不需要碰程式碼，只需要照著步驟操作。

---

## 1. 補照片並重新產生網站

你的照片還在挑，這是你最常會做的事。

1. 把新照片拖進對應地點的來源資料夾。來源資料夾在 `桌面\Soap美西\` 底下，10 個資料夾分別對應 10 個地點（`1.joshua tree`、`2.Grant Canyon`……）。哪個資料夾對應哪個地點，可以查 `data\points.js` 裡每筆資料的 `srcFolder` 欄位。
2. 打開這個資料夾（`美西互動網頁`），對著空白處按住 Shift 右鍵 → 「在此處開啟 PowerShell 視窗」（或用你平常開終端機的方式）。
3. 執行：

   ```
   powershell -ExecutionPolicy Bypass -File scripts\build-photos.ps1
   ```

4. 跑完會印出每個地點現在有幾張照片、新產生了幾張、大小總計多少。**只有新照片會被處理，其他 100 多張不會重跑**，所以就算你只加一張，也是幾秒鐘的事。
5. 想確認結果對不對，可以再跑一次：

   ```
   powershell -ExecutionPolicy Bypass -File scripts\verify.ps1
   ```

   全部顯示 PASS 就代表沒問題。

6. 最後重新部署上線（見第 5 節）。

**注意**：你的照片有些是直拍的（手機或相機直立拍攝），腳本會自動偵測並轉正，你不用自己旋轉照片。

---

## 2. 新增一個地點

比如你之後想把死亡谷、紅杉這些路上經過的地方也加進網站。

1. 在 `桌面\Soap美西\` 底下開一個新資料夾，把那個地點的照片放進去（資料夾名稱隨便你取，中文、空格都可以）。
2. 打開 `data\points.js`，複製其中一筆現有的地點（例如 Napa Valley 那筆），貼在你想要的位置，改成新地點的資料：

   ```js
   {
     "id": "death-valley",              // 隨便取一個英文代號，不能跟其他地點重複
     "srcFolder": "你剛剛開的資料夾名稱",
     "name": "死亡谷",                   // 網站上顯示的中文名稱
     "coords": [36.5323, -116.9325],    // 這個地點的緯度、經度
     "date": "",
     "description": "",
     "showMarker": true,                 // 要不要在地圖上顯示標記
     "inRoute": true                     // 要不要納入藍色自駕路線
   }
   ```

   座標可以上 Google 地圖搜尋那個地點、右鍵點一下地圖，選單第一行就是經緯度，複製貼上即可（記得順序是「緯度, 經度」，跟 Google 顯示的順序一樣）。

3. `showMarker` 跟 `inRoute` 這兩個開關是分開的，四種組合都合法：
   - 都是 `true`：像大峽谷、拉斯維加斯這種，正式的自駕停留點，會有編號標記
   - `showMarker: true`、`inRoute: false`：像 Napa Valley，有標記、有照片，但不在藍色路線上
   - `showMarker: false`、`inRoute: true`：像 Baker、Bealville，純粹用來把路線拉到正確的公路上，地圖上看不到

4. 如果這個新地點要**插入自駕路線中間**（不是加在路線之外），要重新產生路線：

   ```
   powershell -ExecutionPolicy Bypass -File scripts\build-route.ps1
   ```

5. 重新產生照片：

   ```
   powershell -ExecutionPolicy Bypass -File scripts\build-photos.ps1
   ```

6. 跑 `verify.ps1` 確認沒問題，再部署上線（第 5 節）。

**把途經點升級成正式停留點**（例如以後想讓 Baker 也顯示照片）：只要把它的 `showMarker` 從 `false` 改成 `true`，並且補上 `srcFolder` 指向一個有照片的資料夾，就完成了，不需要搬動這筆資料的位置。

---

## 3. 填寫遊記文字

`data\points.js` 裡每個地點都有一個 `description` 欄位，現在全部是空字串 `""`。

想幫某個地點寫幾句話，就直接把文字打進去：

```js
"description": "這裡是我們第一次看到大峽谷的地方，那種震撼很難用照片表達。"
```

存檔、重新產生網站（第 1 節的步驟 2–5），那段文字就會出現在面板裡。**只填部分地點也完全沒問題**，沒填的地點就是純照片牆，不會有任何空白區塊。

同一個地方也可以填 `date` 欄位（例如 `"2026-06-16"`），面板會顯示在標題下面。留空就不顯示。

---

## 4. 調整路線途經點並重跑

如果你發現藍色路線走的跟實際不一樣（例如某段抄了捷徑、漏了一段繞路），做法是**調整途經點清單，不是手動去畫線**。

1. 打開 `data\points.js`，找到 `inRoute: true` 的那些地點——這些就是路線會依序經過的點。陣列的**順序就是路線順序**。
2. 如果路線少繞了一段路，可以插入一個新的途經點（`showMarker: false`、`inRoute: true`，參考現有的 Baker、Bealville、大峽谷（返程）這幾筆的寫法），座標抓在那段你希望路線經過的路上。
3. 存檔後重新產生路線：

   ```
   powershell -ExecutionPolicy Bypass -File scripts\build-route.ps1
   ```

4. 跑完會告訴你新算出來的總里程數，可以跟 Google Maps 上你實際的路線比對一下。
5. 打開網站看看藍線畫得對不對（見下面「本機預覽」）。滿意的話再部署上線。

---

## 本機預覽（部署前先自己看一眼）

改完任何東西，建議先在自己電腦上看過一次，確認沒問題再上線。

```
powershell -ExecutionPolicy Bypass -File scripts\serve.ps1
```

跑起來之後，瀏覽器打開 `http://localhost:8765/` 就能看到最新版本。按 Ctrl+C 或直接關掉那個終端機視窗就會停止。

（也可以直接雙擊 `index.html`，不開任何伺服器一樣能看，只是這種方式在完全沒有網路連線時，衛星底圖跟標準底圖的圖磚會是空白的——這是正常的，程式其餘部分不受影響。）

---

## 5. 重新部署上線

前面不管做了什麼修改（補照片、加地點、改路線、填文字），最後都是同一個步驟：

```
powershell -ExecutionPolicy Bypass -File scripts\deploy.ps1
```

這支腳本會自動：
1. 先跑一次 `verify.ps1` 當安全檢查——資料有問題會直接停在這裡，不會把壞掉的版本送上線
2. 把網站需要的檔案打包好
3. 上傳到 Cloudflare

通常幾秒到十幾秒就會完成，跑完會印出網址 `https://west-usa-2026.pages.dev/`。**這個網址不會變**，每次部署都是更新同一個網站，不會產生新網址。

**如果 `deploy.ps1` 說找不到 `wrangler` 或要你登入**：代表 Cloudflare 的登入資訊過期了，執行 `npx wrangler login`，瀏覽器會跳出來，用你的 Cloudflare 帳號（`Ja604041062@gmail.com`）登入一次，之後就不用再登入了。

---

## 檔案結構速查

```
美西互動網頁/
├── index.html              網站首頁（很少需要改）
├── data/
│   ├── points.js           地點資料 —— 你唯一常需要手動編輯的檔案
│   ├── photos.js           照片清單 —— 自動產生，不要手動編輯
│   └── route.js            路線幾何 —— 自動產生，不要手動編輯
├── photos/                  壓縮後的照片（自動產生）
├── assets/                  網站的程式碼與樣式（不太需要動）
└── scripts/
    ├── build-photos.ps1    重新壓縮照片、產生 photos.js
    ├── build-route.ps1     重新計算路線、產生 route.js
    ├── verify.ps1           檢查資料有沒有問題
    ├── serve.ps1            本機預覽用的小型伺服器
    └── deploy.ps1           打包並上傳到 Cloudflare Pages
```

真正的照片原始檔在專案資料夾**外面**：`桌面\Soap美西\`。這個資料夾不會被放進網站，也不會被推上 GitHub——網站上用的都是壓縮過的版本。

---

## 有問題就找 Claude

如果哪個步驟卡住、腳本報錯、或想加一個這裡沒提到的功能，把這份專案資料夾丟給 Claude Code，說明你想做什麼就可以了。`SPEC.md` 和 `STATUS.md` 這兩個檔案記錄了完整的設計決策和施工過程，能幫助接手的人（包含未來的你）快速搞懂整個專案的來龍去脈。
