# 美西自駕 2026 — 互動遊記地圖 Spec

- **狀態**：已部署上線
- **建立日期**：2026-08-06
- **產出網址**：https://west-usa-2026.pages.dev/（原計畫的 GitHub Pages 網址部署失敗未採用，詳見「部署」一節）
- **程式碼倉庫**：https://github.com/ja604041062/west-usa-2026

---

## Problem Statement

我在 2026 年 6 月中開車走了一趟美西。飛抵洛杉磯的當晚沒有行程，隔天一早出發，一路開過 Joshua Tree、大峽谷、下羚羊谷、馬蹄灣、拉斯維加斯、優勝美地，最後抵達舊金山還車、搭機飛回洛杉磯。自駕全程 1,704 英里、約 29 小時車程。

我拍了大量照片，**目前挑出 108 張、共 926MB**（而且還在繼續挑）。現在它們只是躺在資料夾裡的 `DSC*.JPG` 流水號檔案。這帶來三個問題：

1. **照片脫離了地理脈絡**。看照片的人不知道「這張紅色岩壁是羚羊谷還是大峽谷」、也不知道這兩個地方在美西的哪裡、離多遠。整趟旅程的「路線感」完全消失了。
2. **無法分享**。926MB 的原始檔沒辦法用 LINE 或 email 傳；就算傳得動，對方收到的也只是一堆無序的檔案。
3. **內容還在長**。照片持續增加中——這份 spec 撰寫期間就從 62 張變成 108 張、地點從 7 個變成 10 個。路上經過但目前沒列為停留點的地方（死亡谷、紅杉、Fire Wave、McWay Falls）之後也可能想加進來。**任何「做完就定型」的方案都不能用。**

## Solution

一個**純靜態的單頁互動地圖網站**，程式碼放在 GitHub、網站託管在 Cloudflare Pages 上，有一個可以分享的網址。

打開網頁就是美西地圖，上面有一條藍色路線標出實際開過的路，路線上有 9 個編號圓圈標記（① 洛杉磯 → ⑨ 舊金山），另有 1 個樣式不同的無編號標記代表不在自駕路線上的景點（Napa Valley）。點任何一個標記，畫面右側滑出面板顯示該地點的照片牆；點面板裡的任一張縮圖，進入全螢幕燈箱看大圖，可左右翻頁。

**自駕是這個網站的主軸。** 藍色路線只畫實際開車走過的路段，回程的 SFO → LAX 航班不畫；洛杉磯雖然同時是抵達地與離開地，也只作為單一起點 ① 出現一次。不在自駕路線上的景點以不同樣式的標記呈現，不併入路線。

整個網站是**資料驅動**的：所有地點定義在一個 `points.json` 裡，照片放在使用者自行命名的來源資料夾裡，由 `points.json` 建立對應關係。新增照片 = 把檔案拖進資料夾後重跑一次建置腳本。新增地點 = 在 `points.json` 加一筆 + 開一個資料夾。兩者都不需要修改任何程式碼。

---

## User Stories

### 瀏覽者 — 地圖與路線

1. 作為一個看遊記的人，我想一打開網頁就直接看到地圖，這樣我不用先通過封面或載入動畫才能開始看內容。
2. 作為一個看遊記的人，我想在初始畫面就看到整條路線的全貌（從洛杉磯到舊金山都在畫面內），這樣我能立刻理解這趟旅程的規模。
3. 作為一個看遊記的人，我想看到一條藍色路線連接所有停留點，這樣我知道這是一趟連續的自駕旅行，而不是散落的幾個景點。
4. 作為一個看遊記的人，我想看到路線是沿著真實公路走的（而不是點與點之間的直線），這樣我能感受到實際的行車路徑。
5. 作為一個看遊記的人，我想自駕路線上的每個標記都有編號 ①–⑨，這樣我一眼就知道旅程的先後順序。
6. 作為一個看遊記的人，我想不在自駕路線上的景點用不同樣式、沒有編號的標記，這樣我不會誤以為它是自駕途中的一站。
7. 作為一個看遊記的人，我想不在自駕路線上的景點點下去一樣看得到照片，這樣它不會因為不在路線上就變成二等公民。
8. 作為一個看遊記的人，我想每個標記旁邊有中文地名，這樣我不用懂英文也知道那是哪裡。
9. 作為一個看遊記的人，我想藍色路線只畫在實際開車走過的路段上，這樣「自駕」這條主軸不會被搭飛機或其他方式抵達的地點稀釋。
10. 作為一個看遊記的人，我想在下羚羊谷和馬蹄灣這種距離很近的標記上仍能分別點到各自的照片，這樣不會有標記被另一個蓋住點不到。
11. 作為一個看遊記的人，我想能縮放和拖曳地圖，這樣我可以放大看某個區域的細節。
12. 作為一個看遊記的人，我想有一顆切換鈕可以把底圖換成衛星空照圖，這樣我能看到大峽谷、羚羊谷、沙漠的真實地貌。
13. 作為一個看遊記的人，我想切換底圖時地圖的中心點和縮放層級不變，這樣我不會迷失方向。
14. 作為一個看遊記的人，我想預設看到的是淡色極簡底圖，這樣路線和標記是畫面上最顯眼的東西，不會被地圖本身的顏色干擾。
15. 作為一個看遊記的人，我想在畫面上看到網站標題「美西自駕 2026」和副標「6/12 – 6/23 · 1,704 miles」，這樣我知道這是什麼、什麼時候發生的、規模多大。

### 瀏覽者 — 照片瀏覽

16. 作為一個看遊記的人，我想點擊地圖標記後看到該地點的照片，這樣我能把照片和地理位置連結起來。
17. 作為一個看遊記的人，我想點擊標記後照片出現在側邊面板而不是蓋掉整個畫面，這樣我在看照片時仍然看得到地圖。
18. 作為一個看遊記的人，我想面板從畫面右側滑出，這樣它不會遮住地圖的主要區域。
19. 作為一個看遊記的人，我想面板頂端顯示地點名稱，這樣我確認自己點到的是哪裡。
20. 作為一個看遊記的人，我想面板裡的照片以格狀縮圖排列，這樣我能一次概覽這個地點有哪些照片。
21. 作為一個看遊記的人，我想面板裡的照片欄數會隨著瀏覽器視窗寬度增減，這樣在寬螢幕上不會浪費空間。
22. 作為一個看遊記的人，我想照片依照拍攝先後排列，這樣瀏覽順序符合當天實際發生的順序。
23. 作為一個看遊記的人，我想面板內容超出畫面時可以在面板內捲動，這樣照片多的地點也逛得完。
24. 作為一個看遊記的人，我想點擊縮圖後進入全螢幕燈箱看大圖，這樣我能看清楚照片細節。
25. 作為一個看遊記的人，我想燈箱的背景變暗，這樣我的注意力集中在照片上。
26. 作為一個看遊記的人，我想在燈箱裡用左右箭頭翻到上一張／下一張，這樣我不用退回面板才能看下一張。
27. 作為一個用鍵盤的人，我想用方向鍵翻頁、用 Esc 關閉燈箱，這樣我不用移動滑鼠。
28. 作為一個看遊記的人，我想點燈箱的暗色背景或關閉鈕就能回到面板，這樣我能繼續逛同一個地點的其他照片。
29. 作為一個看遊記的人，我想知道自己在燈箱裡看的是第幾張／共幾張，這樣我知道還剩多少。
30. 作為一個看遊記的人，我想燈箱裡的照片完整顯示在視窗內、不被裁切也不需捲動，這樣我看到的是整張構圖。
31. 作為一個看遊記的人，我想關掉面板後回到完整的地圖，這樣我能接著點下一個地點。
32. 作為一個看遊記的人，我想面板開著的時候仍然能點地圖上的其他標記直接切換地點，這樣我不用先關面板再點。
33. 作為一個看遊記的人，我想面板一開始只載入小張縮圖，只有在我點開燈箱時才載入大圖，這樣我不會為了沒細看的照片等待。
34. 作為一個網路慢的人，我想照片載入時有佔位空間而不是版面亂跳，這樣閱讀體驗不會被打斷。

### 網站作者（我）— 內容維護

35. 作為網站作者，我想把照片拖進以地點命名的資料夾就完成分類，這樣我在挑照片時順手就做完了，不需要額外的標註步驟。
36. 作為網站作者，我想我用來分類的資料夾名稱可以是中文、含空格、含編號前綴、甚至含拼字錯誤的自然命名，不必遷就程式的命名規則，這樣我分類時不用分心。
37. 作為網站作者，我想「這個點要不要顯示標記」和「這個點要不要納入路線」是兩個各自獨立的開關，這樣我能表達「有照片但不在自駕路線上」和「只用來校正路線但不顯示」這兩種情況。
38. 作為網站作者，我想把一個原本只用來校正路線的途經點升級成正式停留點時只要改一個開關，這樣我不用重新設計資料。
39. 作為網站作者，我想有一個 `_unsorted/` 資料夾放我認不出地點的照片，這樣我不會卡在某幾張照片上。
40. 作為網站作者，我想 `_unsorted/` 裡的照片能被辨識出拍攝地點並告訴我該歸到哪個資料夾，這樣我不用自己回想。
41. 作為網站作者，我想跑一次建置腳本就自動完成壓縮和產生照片清單，這樣我不用手動維護任何檔案列表。
42. 作為網站作者，我想建置腳本可以重複執行任意多次，這樣我之後補照片時只要重跑就好。
43. 作為網站作者，我想建置腳本跳過已經壓縮過的照片，這樣重跑時不用等全部重壓一遍。
44. 作為網站作者，我想我新開了一個來源資料夾但忘記在 `points.json` 加對應設定時腳本會警告我，這樣照片不會靜默地從網站上消失。
45. 作為網站作者，我想在 `points.json` 加一筆資料 + 開一個資料夾就能新增停留點，這樣我之後想把死亡谷、紅杉加進來時不用改程式碼。
46. 作為網站作者，我想每個地點都預留一個 `description` 欄位，這樣我之後想寫遊記文字時直接填進去就好。
47. 作為網站作者，我想 `description` 是空的時候面板完全不顯示文字區塊，這樣現階段的純照片牆不會有一塊空白。
48. 作為網站作者，我想只填寫其中幾個地點的文字、其他留空也能正常運作，這樣我可以慢慢寫。
49. 作為網站作者，我想調整途經點清單後重跑腳本就能重新產生路線，這樣路線畫得不符預期時我能快速修正。
50. 作為網站作者，我想路線資料存成獨立檔案而不是寫死在程式裡，這樣我能直接手動微調某一小段座標。
51. 作為網站作者，我想我的原始大圖不要被放進 repo，這樣 repo 不會膨脹到幾百 MB。
52. 作為網站作者，我想 `_unsorted/` 不要被推上 GitHub，這樣未分類的照片不會意外公開。
53. 作為網站作者，我想壓縮後的整站大小控制在 60MB 以內，這樣網頁開得快、GitHub Pages 也不會有問題。
54. 作為網站作者，我想用檔名前綴（如 `01_`、`02_`）就能手動調整照片順序，這樣我能把最好的照片排前面。

### 網站作者（我）— 部署與隱私

55. 作為網站作者，我想有一個可以貼給家人朋友的網址，這樣我不用傳幾百 MB 的檔案。
56. 作為網站作者，我想網站有 `noindex` 設定不被搜尋引擎收錄，這樣不會有陌生人搜到我的家庭照片。
57. 作為網站作者，我想網站不需要密碼就能看，這樣我分享時不用每個人都額外告訴他密碼。
58. 作為網站作者，我想在本機直接雙擊 `index.html` 就能預覽整個網站，這樣我在推上線之前能先確認效果。
59. 作為網站作者，我想網站不依賴任何 CDN，這樣就算 CDN 掛掉或我在沒網路的地方，網站的程式部分仍然能運作。
60. 作為網站作者，我想建置流程不需要安裝 node、npm 或 Python，這樣我不用為了這個專案架設開發環境。

### 瀏覽者 — 開場封面（2026-08-07 新增）

61. 作為一個看遊記的人，我想一打開網頁先看到一張有氣氛的照片和標題，這樣這個網站給我的第一印象像一篇作品，而不是一個功能性的地圖工具。
62. 作為一個看遊記的人，我想在封面上點一顆清楚的「開始」按鈕才進入地圖，這樣我知道接下來要做什麼。
63. 作為一個用手機點開分享連結的人，我想封面在手機螢幕上也能正常顯示、按鈕點得到，這樣我不用特地換成電腦才能看第一眼。
64. 作為一個看遊記的人，我想點「開始」之後很快看到地圖，不用再等待地圖重新載入，這樣封面不會變成惱人的延遲。
65. 作為一個看遊記的人，我想看到藍色路線像正在被畫出來一樣、從洛杉磯延伸到舊金山，這樣我對這趟旅程的「起點到終點」有更強的感受。
66. 作為一個趕時間或已經看過很多次的人，我想點擊畫面或按 Esc 就能跳過開場動畫直接進地圖，這樣我不用每次都被迫看完整段動畫。
67. 作為一個沒注意到可以跳過的人，我想畫面上有個小提示告訴我可以點擊跳過，這樣我不會白白等完整段動畫才意識到本來可以更快。

---

## Implementation Decisions

### 整體架構

- **純靜態網站**，無後端、無資料庫、無建置框架。輸出就是 HTML + CSS + JS + 圖片檔，可以直接用 `file://` 開啟。
- **無 npm、無 node、無 Python**。使用者機器上只有 `git` 和 Windows 內建的 PowerShell / .NET。所有建置工具用 PowerShell 撰寫。
- **資料驅動**：所有可變內容集中在資料檔，程式碼不含任何地點或照片的硬編碼。

### 資料模型

三個由資料層定義、程式層消費的檔案：

1. **地點定義檔（`points.json`）** — 陣列，依旅程順序排列。每筆包含：
   - `id`：地點 slug（如 `grand-canyon`），同時作為壓縮後照片的輸出目錄名
   - `srcFolder`：對應的來源照片資料夾名稱（如 `2.Grant Canyon`）。**與 `id` 分開**，讓使用者能沿用自己習慣的中文／空格／編號前綴命名，不必遷就程式
   - `name`：顯示用中文地名（如 `大峽谷`）
   - `coords`：`[緯度, 經度]`
   - `date`：該地點的日期，可為空
   - `description`：遊記文字，**現階段一律為空字串**；面板僅在此欄位非空時渲染文字區塊
   - `showMarker`：布林值，是否在地圖上顯示標記
   - `inRoute`：布林值，是否納入路線計算

   **`showMarker` 與 `inRoute` 是兩個獨立的維度**，四種組合都有實際用途：

   | `showMarker` | `inRoute` | 意義 | 本專案的例子 |
   |---|---|---|---|
   | ✓ | ✓ | 正式停留點 | 洛杉磯、大峽谷等 9 個 |
   | ✗ | ✓ | 只用來校正路線的途經點 | Baker、Bealville |
   | ✓ | ✗ | 不在自駕路線上的景點 | Napa Valley |
   | ✗ | ✗ | 停用中的點（保留資料不刪） | 目前無 |

2. **照片清單檔（`photos.json`）** — 由建置腳本產生，**不手動編輯**。以地點 `id` 為鍵，值為該地點的照片陣列，每張含縮圖路徑、大圖路徑、原始尺寸（供 lightbox 預留版面用）。

3. **路線幾何檔（`route.json`）** — 由建置腳本產生，內容為 OSRM 回傳的路線座標序列。允許手動編輯以微調個別路段。

### 停留點與途經點

**設計原則：自駕是主軸。** 路線是這個網站的骨架，地點的排序、編號與呈現都服從於「這是一趟開車走完的旅程」。不在自駕路線上的地方以不同方式呈現，避免混淆這條主軸。

**自駕路線上的編號停留點（9 個，`showMarker: true` + `inRoute: true`）**，依序：

| # | 中文名 | 地點 | 來源資料夾 | 張數 |
|---|--------|------|-----------|------|
| ① | 洛杉磯 | Los Angeles, CA | `10.Los Angels` | 17 |
| ② | Joshua Tree | Joshua Tree National Park, CA | `1.joshua tree` | 13 |
| ③ | 大峽谷 | Grand Canyon, AZ | `2.Grant Canyon` | 14 |
| ④ | 下羚羊谷 | Lower Antelope Canyon, Page, AZ | `3.lower antelope canyon` | 5 |
| ⑤ | 馬蹄灣 | Horseshoe Bend, Page, AZ | `4.馬蹄灣` | 2 |
| ⑥ | 拉斯維加斯 | Las Vegas, NV | `5.Las Vegas` | 10 |
| ⑦ | 優勝美地 | Yosemite National Park, CA | `6.Yosemite` | 15 |
| ⑧ | 優勝美地→舊金山 沿途 | 以 Gilroy, CA 為錨點 | `7.yosemite to SFO沿途` | 6 |
| ⑨ | 舊金山 | San Francisco, CA | `8.SFO` | 19 |

**洛杉磯的定位**：實際行程是「飛抵 LA（當晚）→ 隔天一早出發 → 全程自駕 → 舊金山還車搭機飛回 LA」，所以 LA 同時是抵達地與離開地。但**依「自駕為主軸」的原則，LA 只作為單一的起點 ①，不在結尾重複出現，路線終點為 ⑨ 舊金山**。SFO → LAX 的回程航班不畫在地圖上。

**不在自駕路線上的景點（1 個，`showMarker: true` + `inRoute: false`）**：

| 中文名 | 地點 | 來源資料夾 | 張數 |
|--------|------|-----------|------|
| Napa Valley | Napa Valley, CA | `9. Napa Valley` | 7 |

Napa Valley 在舊金山以北，**不納入路線計算，藍色路線不延伸過去**。標記**不給編號**，並使用與編號停留點**不同的樣式**（不同顏色、無數字），以視覺區分「這不是自駕路線的一站」。點擊行為與其他標記完全相同。

**僅影響路線、不顯示標記的途經點（3 個，`showMarker: false` + `inRoute: true`）**：

- `Baker, CA` 與 `Bealville, CA` — 插在 ⑥ 拉斯維加斯與 ⑦ 優勝美地之間，用來把路線壓回實際走的 I-15 → Barstow → CA-58 → Tehachapi 路徑，而不是 OSRM 預設的最快路線。
- `大峽谷（返程）` — 插在 ⑤ 馬蹄灣與 ⑥ 拉斯維加斯之間，座標與 ③ 大峽谷相同。實際行程是從馬蹄灣／下羚羊谷一帶折返回大峽谷、再前往拉斯維加斯，而非從 Page 直接西行。這個途經點讓路線正確畫出這段折返，不需要重複顯示大峽谷的標記（已在 ③ 出現過）。

原先規劃的 `Gilroy, CA` 途經點已**升級為編號停留點 ⑧**（同時具備標記與路線校正兩種作用），不再需要獨立的途經點條目。

所有座標由實作者查證後填入 `points.json`。`Bealville, CA` 是 Kern County 的小地名，地名查詢可能失敗，必須以經緯度直接指定。

**已知的落差，皆為已接受的簡化**：

1. ⑧「優勝美地→舊金山 沿途」是一整段公路而非單一地點，以 Gilroy 作為地圖上的錨點。照片實際拍攝位置可能散布在該路段各處。
2. ④ 下羚羊谷與 ⑤ 馬蹄灣實際距離僅約 10 分鐘車程，在美西全景的縮放層級下兩個標記會非常靠近甚至重疊。實作時需確保兩者仍可分別點擊（例如標記略作偏移、或縮放後自然分開）。

以上任一項若之後不滿意，調整途經點清單重跑腳本即可修正。

**里程與時間**：加入大峽谷返程後，實測總里程為 1,862.5 英里，較 Google Maps 截圖的 1,704 英里多約 9%——這是預期的，因為 Google Maps 的 29 小時／1,704 英里只計算了單程精簡路徑，沒有算入折返段；加回折返段後里程更貼近實際開車距離。時間估算為 39.7 小時，比截圖的 29 小時長，因為 OSRM 不模擬使用者實際開車的節奏（休息、市區穿越等），此落差維持已接受狀態。

### 路線產生

- 使用 **OSRM 公開路徑服務**（開源、免費、免註冊、免 API key）產生開車路線幾何。
- 送入 OSRM 的座標序列為 `points.json` 中**所有 `inRoute: true` 的地點，依陣列順序**。`inRoute: false` 的地點（如 Napa Valley）**完全不參與路線計算**。
- **僅在建置時呼叫一次**，結果寫入 `route.json`。網站執行期間**不對任何外部服務發出請求**。
- 途經點清單改變時重跑腳本即可重新產生路線。

### 地圖與呈現

- **地圖函式庫**：Leaflet。檔案**下載後放進 repo**，不從 CDN 載入 —— 確保離線可測試、不受 CDN 中斷影響。
- **預設底圖**：CARTO Positron（淡色極簡）。理由：讓藍色路線、彩色標記與照片成為畫面焦點。
- **次要底圖**：Esri World Imagery（衛星空照）。透過畫面角落的切換鈕在兩者間切換，切換時保持地圖中心與縮放層級不變。
- **底圖地名為英文**，這是免費圖磚服務的限制，已接受。使用者自訂的 10 個標記標籤為中文。
- **初始視野**：自動計算成能框住整條路線的範圍。
- **標記樣式**：自駕路線上的停留點為帶數字的圓形標記 ①–⑨，旁附中文地名。**不做 hover 預覽照片**。
- **路線外景點的標記樣式**：Napa Valley 使用無數字、顏色不同的圓形標記，旁附中文地名。使用者無需圖例即可看出它與編號標記不屬於同一序列。
- **無行程清單側欄**。順序資訊由標記編號承載。
- **有開場封面與轉場動畫**，詳見下方「開場封面與轉場動畫」一節——這兩項原本在第一版 SPEC 明確排除，2026-08-07 經使用者重新確認後推翻，理由與設計細節見該節。

### 開場封面與轉場動畫

**2026-08-07 新增，推翻第一版 SPEC 的兩項 Out of Scope（開場封面頁、路線繪製動畫）。** 決策過程見對話紀錄；以下是拍板結果。

**目的**：純粹是儀式感／第一印象，不是為了補資訊（標題與副標地圖上本來就有）也不是為了掩蓋底圖圖磚的載入延遲。

- **出現頻率**：**每次打開網站都顯示封面**，不做「只看一次」的例外（不用 `localStorage` 記錄造訪狀態）。這是刻意的取捨：訪客大概率是家人朋友用手機重複點開連結看照片，作者接受「每次都要多一次點擊」的代價，換取「儀式感」不因訪客身份而打折。
- **封面內容**：大標題「美西自駕 2026」＋背景照片＋「開始」按鈕。**不放副標**（日期／里程留給進地圖後再看，封面只負責情緒）。
- **背景照片**：馬蹄灣 `DSC04172`（[STATUS.md](STATUS.md) 108 張照片中，橫向構圖、天空區域乾淨、適合疊字的一張）。
- **文字可讀性**：照片上疊一層半透明深色遮罩（例如 `rgba(0,0,0,0.3~0.4)`），確保標題在任何裝置、任何螢幕亮度下都好讀，不靠純文字陰影/描邊硬撐。
- **手機自適應**：封面本身做基本 RWD（文字大小隨螢幕縮放、背景圖 `object-fit: cover` 撐滿、按鈕可觸控點擊）。**這是「目標裝置」一節既有限制的一個例外**——封面是分享連結後每個訪客看到的第一個畫面，多半經 LINE／email 在手機上開啟；地圖本體（含面板、燈箱）仍維持桌機限定，不做手機版面。
- **地圖背景預載**：封面顯示的同時，Leaflet 地圖與底圖圖磚就在背景初始化，不等使用者點「開始」才開始載入，避免點擊後又要等地圖出現。
- **地圖既有標題不變**：地圖左上角原有的標題／副標區塊維持現狀，不因為封面的出現而拿掉或簡化。這個區塊還兼著底圖切換鈕的容器與 `fitBounds` 動態留白的量測依據，這次不牽動它的既有邏輯。

**轉場（點擊「開始」之後）**：

- **不做運鏡動畫**（不用 Leaflet 的 `flyTo`/`panTo` 疊加時間軸）。鏡頭從一開始就固定在跟「無封面」方案一樣的全景視角。
- 封面先淡出（約 0.3–0.4 秒），淡出結束後**立即**接上路線動畫，中間不額外停留——封面淡出後露出的是已經預載好的靜態全景地圖（標記都在、底圖圖磚都在，只是還沒畫線），所以「淡出→畫線」銜接起來不會有空白停頓的觀感。
- **路線動畫**：藍色路線用 `requestAnimationFrame` 逐幀重畫 `route.json`（`TRIP_ROUTE.coordinates`）的座標子集合，從 LA 端往 SF 端逐步「長出來」；路線上的編號標記依線的進度依序彈出。純粹是重畫同一條 polyline 的座標子集合，不引入任何新的地圖套件，跟現有「`<script>` 標籤、無 build 工具」的架構相容。
- **總時長**：封面淡出＋畫線動畫合計約 5 秒。
- **逃生閥（必要，不可省略）**：動畫播放中，**點擊畫面任一處或按 `ESC`** 直接跳到最終狀態（線畫完、10 個標記全部出現）。這是為了「家人在車上用手機重複點開看照片」這種場景設計的——每次都要完整看完 5 秒動畫會是最容易被抱怨的地方。
- **跳過提示**：動畫播放時顯示一行小字提示（例如「點擊畫面跳過」），動畫結束或被跳過後這行字消失。沒有提示的話，逃生閥形同沒做——多數使用者不會主動去猜「畫面可以點擊跳過」。**實作時選在畫面下方置中**而不是角落——這個提示存在的唯一理由就是要被注意到（尤其是最擔心的「家人在車上用手機」情境），塞在角落反而違背這個目的。

### 面板與燈箱

- **本階段只做桌機**。詳見下方「目標裝置」。
- **面板**：自畫面右側滑出。內容依序為地點名稱、日期（若有）、文字區塊（**僅在 `description` 非空時渲染**）、照片格狀縮圖牆。面板內容過長時於面板內捲動，不捲動整頁。
- 照片牆欄數隨面板寬度以 CSS Grid 自動增減，不使用固定欄數。
- 面板開啟時地圖仍可見且可互動；點擊其他標記直接切換面板內容，不需先關閉。
- **燈箱**：**自行實作，不引入第三方套件**（預估約 80 行）。功能包含暗色遮罩、大圖置中且完整顯示於視窗內（`contain` 而非裁切）、左右翻頁按鈕、頁碼指示、關閉鈕、點遮罩關閉、鍵盤方向鍵與 Esc。**不實作觸控滑動手勢**。
- **照片排序**：依檔名排序。原始檔名 `DSC01686` → `DSC06208` 本身即為拍攝時間順序。使用者可加檔名前綴手動覆寫順序。

### 照片管線

- **來源**：使用者將挑好的照片拖入以地點命名的資料夾。**資料夾名稱由使用者自由決定**（可含中文、空格、編號前綴、拼字錯誤），程式端不對命名做任何假設；`points.json` 的 `srcFolder` 欄位負責建立「資料夾 → 地點」的對應。使用者現行命名如 `2.Grant Canyon`、`4.馬蹄灣`、`9. Napa Valley`，**不要求使用者重新命名**。
- **未分類照片**：認不出地點的丟入 `_unsorted/`，由實作者辨識後回報應歸屬的資料夾（人工分類為主、影像辨識為輔的混合流程）。目前使用者已完成分類，`_unsorted/` 為空，但機制保留供日後補照片時使用。
- **壓縮工具**：PowerShell + Windows 內建 .NET `System.Drawing`。**不需安裝 Python 或任何外部工具**。
- **兩種輸出尺寸**：
  - 縮圖：寬 600px，JPEG 品質 75（面板照片牆使用，每張約 60KB）
  - 大圖：寬 2000px，JPEG 品質 82（燈箱使用，每張約 400KB）
- 原圖為 6192×4128（約 2,560 萬畫素），僅縮小不放大；維持原始長寬比。
- **腳本具冪等性**：重複執行時跳過已存在且較新的輸出檔，只處理新增或變更的照片。
- **孤兒偵測**：來源目錄下若出現不對應任何 `points.json` `srcFolder` 的資料夾，腳本必須警告，避免使用者新增資料夾後照片靜默地不出現在網站上。
- **原始檔不進 repo**。來源照片目錄與 `_unsorted/` 皆由 `.gitignore` 排除，只有壓縮後的輸出進版控。
- **照片無 GPS EXIF**（已查證：GPS 標籤數為 0），因此無法依座標自動分類，資料夾分類是必要的。
- **EXIF 方向修正**（實作時發現、原始票 04 未明確要求，屬必要的補充範疇而非規格外的功能）：實測 108 張照片中有 19 張帶 EXIF 方向標記（相機直拍產生）。`System.Drawing` 不會自動轉正，不處理的話這 19 張會整個躺平顯示。壓縮前先讀 EXIF 方向標記（tag 274）、依標準 8 種方向對照表轉正，`photos.js` 裡的 `w`/`h` 是**修正方向後**的尺寸（直拍照片的寬高因此對調）。

### 部署

- **程式碼託管在 GitHub**（帳號 `ja604041062`，repo `west-usa-2026`），但**網站託管改成 Cloudflare Pages**，網址 `https://west-usa-2026.pages.dev/`。
  - **原計畫是 GitHub Pages**（`https://ja604041062.github.io/west-usa-2026/`），但實際部署時遇到 GitHub Pages 的建置管線卡住：legacy（Jekyll）流程建置失敗，改用 GitHub Actions 的 workflow 流程後，Actions 執行器一直排在佇列裡超過 15 分鐘沒有被排到、最終被系統取消，判斷是帳號/repo 太新導致的執行器指派延遲。GitHub Pages 設定已停用，避免留下一個打不開的公開網址。
  - **改用 `wrangler`（Cloudflare 官方 CLI，經 npm 安裝，因此連帶需要安裝 Node.js）以 Direct Upload 方式部署**：不透過 Git 整合（不授權 Cloudflare 存取 GitHub repo），純粹把建置產物（`index.html`、`assets/`、`data/`、`photos/`、`robots.txt`）複製到一個乾淨的暫存資料夾後上傳，避免 `.git/`、`SPEC.md`、`STATUS.md`、`.scratch/` 等內部文件被誤傳到公開網站上。
  - 每次要更新網站內容，重新執行 `wrangler pages deploy` 上傳同一份乾淨資料夾即可；GitHub repo 仍是程式碼與提交歷史的唯一來源。
- repo 名使用全小寫英文加連字號 —— 中文 repo 名會使網址變成 percent-encoding 亂碼。
- **公開網站但加 `noindex`**：於 HTML 加入 robots meta 標籤，並提供 `robots.txt` 阻擋索引。網址不主動散播。在 Cloudflare Pages 上已實測 `robots.txt` 與 `noindex` meta 皆正常送出。
- **不做密碼保護**。免費方案技術上無法提供（Cloudflare Pages 的 Access 功能可以做到，但需要另外設定，目前未啟用）；加密碼也會讓分享變麻煩。已與使用者確認並接受此隱私取捨。
- 使用者的 `git` 原本已安裝但未設定 `user.name` / `user.email`，且未安裝 `gh` CLI ——首次部署時已設定 git 身份（僅限本機 repo 層級，未動全域設定）、以 `winget` 安裝 `gh` CLI 並透過瀏覽器 OAuth 完成 GitHub 登入；同樣以 `winget` 安裝 Node.js 供 `wrangler` 使用，並透過瀏覽器 OAuth 完成 Cloudflare 登入。全程未要求使用者提供密碼或 token 給實作者。

### 目標裝置

- **本階段僅支援桌機瀏覽器**。版面以滑鼠與鍵盤操作為前提設計。
- **不實作**：手機／平板的專屬版面（面板由底部滑上、頂部橫向清單）、觸控滑動手勢、行動裝置的斷點與測試。
- 這是**延後而非永久排除**。資料層（`points.json` / `photos.json` / `route.json`）與照片管線與裝置無關，日後補做手機版時**不需要改動資料或重新處理照片**，只需增加樣式斷點與觸控事件。
- 實作時避免寫入會阻礙日後加入手機版的決定：不使用固定像素寬度的版面、不假設有滑鼠 hover 才能觸發的必要功能、不在 JS 中硬編碼視窗尺寸。
- 驗收基準解析度：1920×1080 與 1440×900。

### 網站識別

- 標題：`美西自駕 2026`
- 副標：`6/12 – 6/23 · 1,704 miles`
- 介面語言：繁體中文

---

## Testing Decisions

**這個專案沒有既有的測試框架，也沒有 node/npm 可以架設一個。** 引入測試執行環境的成本會超過這個規模的靜態網站所能得到的效益。因此測試策略以**建置腳本的可驗證輸出**與**明確的人工驗收清單**為主。

### 好的測試長什麼樣

只驗證**外部可觀察的行為**，不驗證實作細節。對這個專案而言：

- 驗證「建置腳本產出的 `photos.json` 涵蓋每個資料夾裡的每張照片」，而不是驗證腳本內部怎麼走訪目錄。
- 驗證「點擊標記後面板顯示了正確地點的照片」，而不是驗證某個 DOM class 名稱。
- 驗證「路線資料的起點在洛杉磯附近、終點在舊金山附近」，而不是驗證 OSRM 回傳的每一個座標。

### 測試接縫（Seams）

**理想接縫數量是一個。這個專案自然形成一個主接縫：**

> **建置腳本的輸入（來源照片目錄樹 + `points.json`）與輸出（`photos.json` + `route.json` + 壓縮後圖片檔）之間。**

這是最高的接縫 —— 網站的執行期行為完全由這三項輸出決定，只要輸出正確，前端就有正確的資料可以渲染。在此接縫上做**驗證式測試**：

1. **完整性**：`points.json` 中每個 `showMarker: true` 的地點，在 `photos.json` 中都有對應項目；每個來源資料夾裡的每個檔案都在輸出中出現，數量相符（逐地點比對，不對總數設固定值——照片會持續增加，寫死總數的斷言在下次補照片後就會誤報失敗，實作時已改成「總數非零」的健全性檢查 + 資訊性輸出目前的實際張數）。撰寫本節當下的分類為 洛杉磯 17、Joshua Tree 13、大峽谷 14、下羚羊谷 5、馬蹄灣 2、拉斯維加斯 10、優勝美地 15、沿途 6、舊金山 19、Napa Valley 7，合計 108 張，僅供參考。
2. **產物存在性**：`photos.json` 引用的每個縮圖與大圖路徑，檔案都確實存在於磁碟上。
3. **尺寸正確性**：抽樣輸出圖片，確認縮圖寬度為 600px、大圖寬度為 2000px（或原圖寬度，若原圖較小），且長寬比與原圖一致。
4. **大小預算**：輸出目錄總大小低於 60MB。超過即為失敗，需調整壓縮參數。（依 108 張照片、每張縮圖約 60KB + 大圖約 400KB 估算，預期約 50MB。照片數量若再增加需重新檢視此預算。）
5. **冪等性**：連續執行腳本兩次，第二次不應改變任何輸出檔的內容。**這項刻意不做進 `verify.ps1` 的自動化檢查**——`verify.ps1` 設計成純粹唯讀，檢查資料現狀，不應該有「為了驗證而順便重新建置」這種副作用；把建置動作塞進驗證腳本會混淆兩者的職責。這項改為人工／情境測試：實作過程中已手動驗證多次（新增一張測試照片重跑、確認只處理新增的那張且其餘全部略過、再移除測試照片復原），記錄在 `STATUS.md`。
6. **路線健全性**：`route.json` 的座標序列非空、起點鄰近洛杉磯、終點鄰近舊金山、且經過每一個 `inRoute: true` 的地點附近。座標皆落在合理的美西經緯度範圍內。
7. **路線排除性**：`route.json` **不得**經過任何 `inRoute: false` 的地點。具體而言，路線不應延伸至舊金山以北的 Napa Valley 一帶。這條檢查是「自駕為主軸」這項設計原則的自動化守門員。
8. **孤兒偵測**：來源目錄下若有資料夾不對應任何 `points.json` 的 `srcFolder`，腳本應警告而非靜默忽略。
9. **對應完整性**：`points.json` 中每個有 `srcFolder` 的地點，該資料夾都確實存在（防止拼字錯誤造成某個地點靜默地沒有照片）。

這些檢查以 PowerShell 撰寫，作為建置腳本之後可獨立執行的驗證腳本。

### 人工驗收清單

前端互動行為（面板滑出、燈箱翻頁、底圖切換）沒有自動化測試環境，改以固定的人工驗收清單覆蓋，每次改動後跑一遍。**本階段僅在桌機瀏覽器上驗收**：

- 在 1920×1080 與 1440×900 兩種視窗尺寸各開一次，確認初始視野框住整條路線與 Napa Valley 標記、版面無破圖或水平捲軸
- 依序點擊 ①–⑨ 每個標記，確認面板顯示正確地點與照片
- 點擊 Napa Valley 標記，確認樣式與編號標記明顯不同、但點擊後照片正常顯示
- 確認藍色路線**沒有**延伸到 Napa Valley
- 放大到 Page 一帶，確認下羚羊谷與馬蹄灣兩個標記可分別點擊、不互相遮蔽
- 面板開啟時直接點另一個標記，確認內容切換且不需先關閉
- 確認照片牆欄數隨視窗寬度變化，且面板內容過長時在面板內捲動
- 開啟燈箱，測試左右箭頭、鍵盤方向鍵、Esc、點遮罩關閉、頁碼顯示
- 確認燈箱大圖完整顯示於視窗內、未被裁切
- 切換衛星底圖，確認地圖中心與縮放層級不變
- 以 `file://` 直接開啟 `index.html`，確認在無網路狀態下程式部分仍可運作（底圖圖磚會空白，屬預期）
- 確認 `description` 為空時面板不出現空白文字區塊；手動填入一段文字後確認文字區塊出現
- **開場封面（新增）**：重新整理頁面，確認每次都會先看到封面（大標題＋馬蹄灣照片＋開始按鈕），不會因為造訪過就跳過
- 點「開始」，確認封面淡出後緊接著播放路線動畫（藍線從 LA 往 SF 生長、標記依序彈出），全程無明顯停頓或閃爍
- 動畫播放中點擊畫面任一處，確認直接跳到最終狀態（路線畫完、10 個標記全部顯示）；重新整理後改用 `Esc` 鍵測試一次，效果相同
- 確認動畫播放時角落有「點擊跳過」提示文字，動畫結束或被跳過後提示文字消失
- 將瀏覽器視窗縮到手機寬度（例如 375px），確認封面文字與按鈕仍清楚可讀、可點擊，背景照片撐滿畫面不變形
- 確認封面上的文字在半透明遮罩之下，於畫面各處都清楚可讀（天空最亮處與岩壁最暗處都測一次）
- 確認地圖左上角原有的標題／副標仍正常顯示，副標數字已更新為「6/12 – 6/23 · 1,704 miles」

### 既有相關實作

專案內**無既有測試可參考**。桌面上有兩個既有的靜態 HTML 檔案（`index.html`、`stock.html`），與本專案無關，不作為參考基準。

---

## Out of Scope

- **手機與平板版面**。本階段只做桌機。包含行動裝置專屬版面、觸控滑動手勢、以及行動裝置上的驗收。屬**延後**而非永久排除，資料層與照片管線皆與裝置無關，日後補做不需重工。
- **遊記文字內容**。現階段是純照片牆。`description` 欄位預留但一律留空，面板不渲染文字區塊。之後填寫時無需修改程式碼。
- **每張照片的圖說**。照片數量會持續增加，逐張寫圖說是無底洞。
- **側邊行程清單**。已明確排除，順序資訊由標記編號承載。
- **標記的 hover 照片預覽**。
- **中文地名底圖**。需要 Google 或 Mapbox 的帳號與 API key，不值得。底圖維持英文地名。
- **密碼保護 / 私密網站**。GitHub Pages 免費版做不到，且與分享便利性衝突。
- **WebP 格式**。JPEG 在此規模下已足夠；為節省約 5MB 而引入 Python 執行環境不划算。
- **依 EXIF 時間自動分類照片**。照片無 GPS，且同一天常跨兩個地點，自動分類會出錯。改用資料夾人工分類。
- **死亡谷、紅杉／薛曼將軍樹、Fire Wave、McWay Falls、Oakhurst 作為獨立停留點**。這些地方出現在原始的 Google Maps 路線圖上，但使用者的照片分類中沒有對應資料夾，**本次不納入**。架構已預留擴充能力（加一筆 `points.json` + 開一個資料夾即可升級為正式停留點）。
- **回程航班 SFO → LAX**。洛杉磯只作為起點 ① 出現一次，不在結尾重複。依「自駕為主軸」的原則，非自駕的移動不畫在地圖上。
- **Napa Valley 納入自駕路線**。已明確排除——它有標記、有照片，但藍色路線不延伸過去。
- **原始大圖的線上瀏覽或下載**。網站只提供壓縮版，原始檔留在本機。
- **多語言介面**。僅繁體中文。
- **留言、按讚、訪客統計等社群功能**。
- **CI / 自動部署**。手動跑腳本、手動 push。

---

## Further Notes

### 已拍板事項

- **動工順序**：**現在就用手上的 108 張照片建站並部署**，之後補照片時重跑腳本更新。使用者將此決定授權給實作者判斷。理由：整套流程本身就設計成可重跑，等照片挑完只是延後看到成品的時間，且會讓版面問題拖到最後才發現。
- **部署時機**：部署**提前到第二張票**，而非排在最後。使用者的 `git` 從未設定過身份、機器上無 `gh` CLI，帳號驗證是本專案唯一無法預先確認的未知數。先用骨架打通整條部署路徑，之後每張票完成後僅需再推一次。連帶將 `noindex` 與 `robots.txt` 提前至第一張票，確保網站自首次上線起即不被搜尋引擎收錄。

### 工作拆解

已拆為 8 張票，存放於 `.scratch/west-usa-2026/issues/`。依賴順序：01 骨架與標記 → 02 早期上線；01 → 03 路線 / 04 照片管線 / 07 底圖與識別（三者可並行）；04 → 05 面板 → 06 燈箱；02 + 03 + 06 + 07 → 08 完整驗收。

**2026-08-07 新增 3 張票**，是網站上線後的第一次功能新增，不是原始規劃的一部分：

- **09 開場封面靜態畫面**（`09-cover-static.md`）——Blocked by：無，可立即開始
- **10 地圖背景預載與路線動畫轉場**（`10-cover-transition-animation.md`）——Blocked by：09、03（路線資料）
- **11 完整驗收與重新部署**（`11-final-verification-and-redeploy.md`）——Blocked by：09、10

地圖副標的日期修正（`6/13–6/19` → `6/12–6/23`）與封面功能無關，屬於順手的資料修正，**未另開票，已直接修改 `index.html`**。

### 已查證的環境事實

- 照片來源：`桌面/Soap美西/`，**108 張 JPG，總計 926MB**，已由使用者分入 10 個資料夾，`_unsorted/` 為空、來源目錄根層無散落檔案。
- 單張規格：6192×4128（約 2,560 萬畫素），5–12MB。
- **無 GPS EXIF**（已實測，GPS 標籤數為 0）。
- 使用者的資料夾命名含編號前綴、中英夾雜與拼字差異（`2.Grant Canyon` 應為 Grand Canyon、`10.Los Angels` 應為 Los Angeles、`9. Napa Valley` 編號後有空格）。**這些一律不要求使用者更正**，由 `srcFolder` 對應吸收。
- 使用者資料夾的編號順序（1 = Joshua Tree ⋯ 10 = Los Angeles）**與網站的顯示順序不同**，因為 LA 在行程中同時是抵達地與離開地。網站順序以 `points.json` 的陣列順序為準，**不從資料夾名稱推導順序**。
- 檔案時間戳記橫跨 6/14 – 6/20，相機時鐘研判設定為台灣時區（美西時間需減 15 小時），換算後**有拍照片的天數**約為美西時間 6/13 – 6/19。有一張 `DSC01686.JPG` 的時間戳記為 8/2，屬離群值，研判為事後重存所致，不影響排序（排序依檔名而非時間）。
- **2026-08-07 修正**：網站副標實際採用的完整行程天數是 **6/12 – 6/23**，比上述 EXIF 推算的 6/13 – 6/19 頭尾各多一天。原因是 EXIF 只能反推「有拍照片」的天數，抵達當晚與最後一天退房未拍照的移動日不會被算進去；6/12 – 6/23 是使用者確認過的完整行程範圍（含頭尾的搭機／移動日）。這不是新發現的落差，是先前查證時使用者的照片還在 review 中、樣本不完整所致。
- 開發環境：僅有 `git`（未設定使用者身份）。**無 node、無 npm、無 `gh` CLI**；`python` 為 Windows Store 的空殼啟動器，不可用。
- 專案目錄除 `.claude/`、本 spec、路線截圖外為空，非 git repo。

### 內容規模的變動紀錄

這份 spec 撰寫期間，內容規模已經變動過一次：**62 張 → 108 張、525MB → 926MB、7 個地點 → 10 個地點**。使用者明確表示照片仍在挑選中。

這證實了 Problem Statement 第 3 點不是假設性的顧慮，而是**已經發生的事實**。實作時應把「規模與地點數量會再變」當作硬需求看待，任何寫死張數、寫死地點數、寫死順序的做法都會很快失效。

### 路線的原始依據

路線點位取自使用者提供的 Google Maps 截圖（`螢幕擷取畫面 2026-08-05 001219.png`）。該截圖顯示 1,704 英里、29 小時，行程點清單為 Best Western Plus Commerce Hotel → Joshua Tree → The Grand Hotel at the Grand Canyon → Dixie's Lower Antelope Canyon Tours → 回大峽谷 → Durango Casino & Resort → Oakhurst Grill → Pier 39 海獅觀景區。

本 spec 刻意**不沿用**這些飯店與餐廳名稱作為停留點名稱，改用使用者指定的城市／國家公園層級名稱 —— 遊記的骨架是「地方」而非「住宿」。

該截圖同時是**目前唯一的路線真實性依據**，但它與最終的地點清單有兩處已知落差，兩者都是刻意的：

- 截圖的路線**不含 Napa Valley**（它在舊金山之後、且不在自駕主軸上），本 spec 也不把它畫進路線。
- 截圖的路線**不含馬蹄灣**（它與下羚羊谷同在 Page 一帶，Google Maps 未單獨列出），本 spec 將它列為獨立停留點 ⑤ 並納入路線。

`Baker, CA` 與 `Bealville, CA` 這兩個途經點由使用者直接指定，用途是把 OSRM 的路線壓回實際走的 I-15 → Barstow → CA-58 → Tehachapi 路徑。這條路**不經過死亡谷**——早期依截圖所做的「穿越死亡谷」推測已由使用者更正。

### 尚未落實此 skill 的兩項要求

1. **未發布至 issue tracker**。專案尚無 issue tracker、無 repo、無 `gh` CLI。Spec 以本機檔案形式產出。GitHub repo 建立後應將本文件轉為 issue 並套用 `ready-for-agent` 標籤。
2. **測試接縫未經使用者事前確認**。使用者要求停止提問直接產出 spec，故接縫設計直接寫入本文件，待使用者審閱。
