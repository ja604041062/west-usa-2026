# 施工進度

**最後更新**：2026-08-06
**狀態**：暫停於票 03 開工前

---

## 進度總覽

| 票 | 內容 | 狀態 |
|---|---|---|
| 01 | 專案骨架、地圖與 10 個標記 | ✅ **完成**，已 commit `af06f71` |
| 02 | 早期上線：打通 GitHub Pages | ⬜ 未開始 |
| 03 | 自駕路線的產生與繪製 | ⬜ **未開始 —— 下一步從這裡繼續** |
| 04 | 照片壓縮管線 | ⬜ 未開始 |
| 05 | 側邊面板照片牆 | ⬜ 未開始 |
| 06 | 全螢幕燈箱 | ⬜ 未開始 |
| 07 | 底圖切換與網站識別 | ⬜ 部分完成（標題與副標已做，底圖切換未做） |
| 08 | 完整驗收與上線 | ⬜ 未開始 |

票的完整內容在 `.scratch/west-usa-2026/issues/`。

---

## 目前可運作的東西

雙擊 `index.html`，或跑預覽伺服器後開 `http://localhost:8765/`，會看到：

- 美西地圖，CARTO Positron 淡色底圖
- 9 個藍色編號標記 ①–⑨，依行程順序
- 1 個琥珀色無編號標記（Napa Valley），視覺上明顯不同類
- 每個標記旁有中文地名
- 初始視野自動框住全部 10 個標記
- 左上角網站標題「美西自駕 2026 / 6/13 – 6/19 · 1,704 miles」

**已目視確認**：放大到 Page 一帶時，④ 下羚羊谷與 ⑤ 馬蹄灣清楚分開、可分別點擊；馬蹄灣的座標正確落在科羅拉多河的馬蹄形彎道上。

**還沒有**：藍色路線、照片、面板、燈箱、底圖切換鈕。點標記目前沒有任何反應。

---

## 驗證現況

```bash
powershell -ExecutionPolicy Bypass -File scripts\verify.ps1
```

最後一次執行結果：**通過 13、失敗 2**。

兩個失敗都是**預期中的**，因為對應的票還沒做：

- `photos.js 存在` —— 等票 04
- `route.js 存在` —— 等票 03

票 01 相關的 13 項檢查全部通過，含孤兒偵測（確認 `points.js` 的 `srcFolder` 對應與你 10 個實際資料夾完全吻合）。

---

## 施工中做出的決定（尚未回寫 SPEC.md）

這幾項是實作時才浮現的問題，我當場決定了，**你如果不同意可以推翻**：

1. **資料檔用 `.js` 而非 `.json`**
   原因：`fetch()` 在 `file://` 下會被 CORS 擋掉，而「雙擊 `index.html` 就能看」是硬需求。
   檔案內容是 `window.TRIP_POINTS = { ... };`，手動編輯的體驗和 JSON 幾乎一樣。
   影響：SPEC 與票裡寫的 `points.json` / `photos.json` / `route.json` 實際上是 `.js`。

2. **縮放控制項移到左下角**
   原因：Leaflet 預設放左上角，會壓到網站標題。
   左上角給標題、右上角留給底圖切換鈕（票 07）、左下角給縮放。

3. **地名標籤畫在標記「底下」，且不攔截點擊**
   原因：美西全景下 10 個中文標籤必然互相重疊。原本標籤蓋住了 ③⑤⑦ 三個標記，點不到。
   重疊的標籤只是不好看，被蓋住的標記是壞掉的，所以讓標籤退到後面。

4. **新增 `scripts/serve.ps1` 本機預覽伺服器**
   網站本身不需要伺服器，這支只是開發時驗收互動行為用的。不影響部署。

5. **所有 `.ps1` 必須存成 UTF-8 with BOM**
   原因：PowerShell 5.1 預設用 ANSI 讀 `.ps1`，中文註解會被吃掉導致字串沒收尾、整份腳本解析失敗。
   **之後任何人新增或編輯 PowerShell 腳本都要注意這件事。**

---

## 尚未解決 / 需要你決定

1. **全景下的標籤擁擠**
   10 個中文地名在初始視野下會互相重疊（見票 01 的驗收截圖）。目前的處置是「標記永遠在上、標籤可以被壓住」，功能上沒問題，但視覺上有點亂。
   可選的改法：低縮放層級時只顯示編號、放大後才顯示地名。這會改變第一眼的觀感，所以我沒有自作主張。

2. **`points.js` 裡所有 `date` 欄位都是空的**
   我沒有可靠依據判斷每個地點是哪一天，**刻意不編造**。你之後可以自己填，格式自由。填了面板就會顯示，不填就不顯示。

3. **票 02（部署）需要你參與**
   要推上 GitHub 需要帳號驗證（token 或 SSH），這一步我不能代你做。而且推上去等於**把照片公開到網路上**，我會在動作前先跟你確認。

---

## 環境狀態

- **git**：repo 已初始化，有 1 個 commit。`user.name` / `user.email` 只設在**這個 repo 內**（`liuwe` / `ja604041062@gmail.com`），**沒有動你的全域設定**。
- **遠端**：尚未設定，尚未推送到 GitHub。
- **預覽伺服器**：可能還在背景執行（port 8765）。要停止就關掉那個 PowerShell 視窗，或重開機。重新啟動的指令見下。

---

## 下次從這裡繼續

下一步是**票 03：自駕路線**。具體要做的：

1. 寫 `scripts/build-route.ps1`：讀 `points.js` 中 `inRoute: true` 的 11 個地點（含 Baker、Bealville 兩個隱形途經點），依序呼叫 OSRM 公開路徑服務，把回傳的路線幾何存成 `data/route.js`
2. 在 `app.js` 中畫出藍色折線
3. 跑 `verify.ps1`，讓路線相關的 6 項檢查轉綠——特別是**「路線不得延伸到 Napa Valley」**這條自駕主軸守門員
4. 把產生的路線和你的 Google Maps 截圖比對，回報落差給你確認

之後依序：票 04 照片管線 → 05 面板 → 06 燈箱 → 07 底圖切換 → 02/08 部署與驗收。

**啟動預覽伺服器**：

```bash
powershell -ExecutionPolicy Bypass -File scripts/serve.ps1
```

**跑驗證**：

```bash
powershell -ExecutionPolicy Bypass -File scripts/verify.ps1
```
