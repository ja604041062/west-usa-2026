# 施工進度

**最後更新**：2026-08-07
**狀態**：**8 張票全部完成**。網站上線中：https://west-usa-2026.pages.dev/

---

## 進度總覽

| 票 | 內容 | 狀態 |
|---|---|---|
| 01 | 專案骨架、地圖與 10 個標記 | ✅ **完成**，已 commit `af06f71` |
| 02 | 早期上線：打通 GitHub Pages | ✅ **完成，但改走 Cloudflare Pages**（原因見下），已 commit `b55e513` |
| 03 | 自駕路線的產生與繪製 | ✅ **完成**，已 commit `4a28ebc`；路線折返修正 `cb50c71` |
| 04 | 照片壓縮管線 | ✅ **完成**，已 commit `e102b56` |
| 05 | 側邊面板照片牆 | ✅ **完成**，已 commit `8e0ab81` |
| 06 | 全螢幕燈箱 | ✅ **完成**，已 commit `ca47f19` |
| 07 | 底圖切換與網站識別 | ✅ **完成**，已 commit `31b8044` |
| 08 | 完整驗收與上線 | ✅ **完成**，已 commit `d07f7c5`（部署腳本）、`019c171`（使用說明） |

**網站已經可以分享了**：https://west-usa-2026.pages.dev/

**唯一還沒打勾的一項**：票 08 要求「使用者確認照著說明可以自己操作」——這件事只有你能確認，我沒辦法自我驗證。有空的話照著 `使用說明.md` 試著跑一次補照片或改遊記文字的流程，看看順不順。

票的完整內容在 `.scratch/west-usa-2026/issues/`。

---

## 目前可運作的東西

雙擊 `index.html`，或跑預覽伺服器後開 `http://localhost:8765/`，會看到：

- 美西地圖，CARTO Positron 淡色底圖
- 9 個藍色編號標記 ①–⑨，依行程順序
- 1 個琥珀色無編號標記（Napa Valley），視覺上明顯不同類
- 每個標記旁有中文地名
- 初始視野自動框住全部 10 個標記
- 左上角網站標題「美西自駕 2026 / 6/13 – 6/19 · 1,704 miles」
- **藍色自駕路線**，從洛杉磯連到舊金山，沿真實公路而非直線，且正確畫出馬蹄灣折返大峽谷再去 Vegas 的路段

**已目視確認**：放大到 Page 一帶時，④ 下羚羊谷與 ⑤ 馬蹄灣清楚分開、可分別點擊；馬蹄灣的座標正確落在科羅拉多河的馬蹄形彎道上。路線完整描出行程，Napa Valley 標記明顯獨立在藍線之外。EXIF 直拍照片（108 張中有 19 張）已修正轉正，目視確認過。

**壓縮輸出**：`photos/` 目錄下 108 張照片、216 個檔案（縮圖+大圖）、共 50.5MB。

- **點任一標記（含 Napa Valley）會從右側滑出面板**，顯示地點名稱、日期（若有）、遊記文字（若有）、照片格狀縮圖牆
- 面板開啟時直接點另一個標記會切換內容，不用先關閉；地圖全程可互動
- 面板只載入縮圖，不載入大圖（大圖留給票 06 燈箱用）
- 內容過長時只在面板內捲動，頁面本身不會出現捲軸

**已用瀏覽器實測**：點大峽谷開面板顯示 14 張照片；用程式觸發切到 Napa Valley 顯示 7 張、面板不用先關閉；關閉鈕正常；暫時把大峽谷的 `date`/`description` 填字驗證區塊會出現，測完已還原成空字串（沒有留在你的資料裡）。

**點縮圖現在會開燈箱**：真實滑鼠點擊縮圖後開啟，載入的是大圖（`photo.large`）不是縮圖；左右按鈕、鍵盤方向鍵都能翻頁，第一張往前翻會繞到最後一張、最後一張往後翻會繞回第一張；關閉鈕、點暗色背景、Esc 鍵三種方式都能關；關閉後面板的捲動位置不會被重置。

**標題區塊下面現在有底圖切換鈕**（標準地圖／衛星影像兩顆按鈕）。切換純粹換圖層，地圖中心點與縮放層級完全不受影響（已用程式化事件驗證往返切換座標一致）。衛星影像下路線和標記都還清晰可辨。

**這張票順便修了一個自己帶出來的小回歸**：加了切換鈕之後標題區塊變高，原本置中對稱的地圖留白算法會讓 Napa Valley 的標記被蓋在標題底下看不到。改成左上角多留空間、右下角維持原樣的非對稱留白，10 個標記現在都確認不會被標題或切換鈕蓋住。

**功能面到這裡全部做完了**——只剩部署（票 02）和最終驗收（票 08）。

---

## 票 08：完整驗收與上線

**上線確認**：repo 內確認沒有 `_unsorted`、沒有原始大圖（所有 jpg 都在 `photos/*/thumb` 或 `photos/*/large` 底下，最大檔案 1.46MB，遠小於原始檔的 5–12MB）。線上版本 console 無錯誤，`robots.txt` 與 `noindex` meta 都正常送出。

**正式桌機尺寸驗收（1920×1080、1440×900）**：這次遇到一個值得記錄的插曲——

我原本以為之前幾張票裡「截圖偶爾縮成一張看不清楚的小圖」是沙盒瀏覽器工具的固有限制，這次深入查了一下：先發現面板關閉時用 `transform: translateX(100%)` 位移到畫面外，這個位移會被 `document.documentElement.scrollWidth` 算進去（變成 2400px，超出 1920px 的視窗寬度），懷疑是我們自己的 CSS 造成頁面「實質上」溢出。但進一步驗證發現：**這不是真的 bug**——用 `clientHeight === innerHeight` 這個更準確的判斷方式（捲軸如果真的顯示出來會佔用高度）確認了**沒有任何捲軸真的顯示出來**，滑鼠滾輪、觸控板、鍵盤這些正常操作路徑也都被 `overflow:hidden` 擋住了；`scrollWidth` 只是瀏覽器對 `transform` 位移元素的一個不影響實際顯示的內部計算細節。接著把面板整個藏起來（`display:none`）讓 `scrollWidth` 精確等於視窗寬度，截圖**還是一樣縮小**——這證實了縮小截圖的原因跟我們的網站完全無關，就是這個沙盒工具在明確指定 1920×1080 這類尺寸時自己的限制。

結論：**正式尺寸的驗收改用 DOM 層級的精確斷言完成**（比肉眼看一張可能失真的截圖更可靠）：
- 1920×1080 與 1440×900 下，`clientHeight === innerHeight`（無水平捲軸）
- 全部 10 個標記都落在視窗可視範圍內（初始視野正確框住）
- 標題／切換鈕跟任何標記都沒有重疊
- Napa Valley 標記的 CSS class 跟編號標記不同
- 燈箱大圖的 `getBoundingClientRect()` 完全落在視窗邊界內，兩個尺寸下都確認過（1440×900 時圖片 1161×774、1920×1080 時圖片 1394×929，皆未超出視窗）
- 面板寬度在兩個尺寸下都是 480px（觸頂 `min(480px, 92vw)` 上限），欄數理應一致——確認是 3 欄，符合設計

**唯一沒能用截圖親眼確認的**：面板/燈箱疊層在畫面上「看起來」對不對——沙盒工具的截圖在有疊層效果時似乎不太可靠（我另外測過一次，DOM 屬性全部正確——背景色 `rgba(0,0,0,0.92)`、z-index 900、圖片 src/opacity/display 都對——但截圖畫面看起來像沒套用暗色遮罩）。**這件事我沒辦法自己排除，建議你有空自己在瀏覽器上點開一次面板跟燈箱看一眼**，理論上該對，但眼見為憑。

**端到端重跑驗證**：複製一張既有照片模擬「使用者拖了一張新照片進資料夾」，重跑 `build-photos.ps1`，正確偵測到新增（大峽谷 14→15 張），**且只處理了那 1 張新的，其他 108 張全部跳過**——同時證明「偵測新照片」與「不重複處理」兩件事，過程中沒有改動任何程式碼。測完已刪除測試檔並重跑腳本，確認 `data/photos.js` 精確還原到跟 git 記錄一致的內容（唯一差異是檔案開頭自動產生的時間戳記註解，這是預期中的，不影響資料本身）。

`file://` 直接開啟（不透過任何伺服器）也重新測試過：`window.TRIP_APP`、10 個標記、地圖視野、路線、照片清單全部正常載入，點開大峽谷面板顯示 14 張照片，圖片路徑正確解析成本機相對路徑。

**部署腳本**：把手動部署的步驟寫成 `scripts/deploy.ps1`，會先跑 `verify.ps1` 當安全檢查（資料有問題直接停止，不會把壞掉的版本送上線），再打包、上傳。已實跑一次，`wrangler` 用內容雜湊比對，230 個檔案裡只有 2 個真的變動過的被重新上傳，1.24 秒完成。

**操作說明**：`使用說明.md` 已交付，涵蓋補照片重跑、新增地點（含途經點升級）、填寫遊記文字、調整路線途經點、重新部署上線五件事。**唯一沒打勾的驗收項目**是「使用者確認照著說明可以自己操作」——這件事我沒辦法自我驗證，需要你實際試跑過一次才算數。

---

## 部署過程：GitHub Pages 卡住，改用 Cloudflare Pages

**網站現在的正式網址是 https://west-usa-2026.pages.dev/**，不是 SPEC 原本規劃的 `ja604041062.github.io/west-usa-2026`。原因記錄如下。

**先做的事**（都成功了）：
1. 用 `winget` 裝了 **GitHub CLI（`gh`）**，你透過瀏覽器登入（一次性代碼 + OAuth 授權），我完全沒經手你的帳密
2. 本機 git 分支從 `master` 改名成 `main`
3. `gh repo create` 建立 `west-usa-2026` repo（public）並推上第一批 commit

**卡住的地方**：GitHub Pages 預設用一套叫 **legacy（Jekyll）** 的建置系統，第一次建置就失敗（錯誤訊息只有籠統的「Page build failed」，猜測是 Jekyll 誤解析了某些檔案）。加了 `.nojekyll` 檔案想跳過 Jekyll，改成用 **GitHub Actions** 的新式部署方式後，Actions 的執行器**在佇列裡卡了超過 15 分鐘、一次都沒被排到，最後被系統自動取消**。查了一下：這不是額度問題（公開 repo 的 GitHub Pages 和 Actions 都無限額度免費），比較像是全新帳號/repo 觸發了 GitHub 的執行器指派延遲或反濫用機制。

**你當時的判斷是不要繼續等**，改用別的方案。你也問了照片放到別家公司會不會有隱私疑慮——答案是：**風險跟你原本同意的 GitHub 方案一樣**（一樣是公開但 `noindex` 低調的網址，沒有密碼保護），沒有變得更差；而且部署方式是直接上傳檔案，不需要把 GitHub repo 的存取權限交給 Cloudflare。

**改用 Cloudflare Pages 的過程**：
1. `winget` 裝 **Node.js**（`wrangler` 這個 Cloudflare 官方 CLI 工具需要 npm 才能跑）
2. `wrangler login` 一樣是瀏覽器 OAuth，你自己登入 Cloudflare（`Ja604041062@gmail.com` 這個帳號），我沒經手密碼
3. 上傳前，先把公開網站真正需要的檔案（`index.html`、`assets/`、`data/`、`photos/`、`robots.txt`）複製到一個乾淨的暫存資料夾，**刻意排除** `.git/`、`SPEC.md`、`STATUS.md`、`.scratch/` 這些內部文件，避免不小心把規劃過程、決策記錄也傳上公開網站
4. `wrangler pages deploy` 直接上傳，**7 秒**傳完 230 個檔案，網站幾秒內就能開

**已用瀏覽器實測線上版本**：首頁地圖、路線、10 個標記正常；點大峽谷標記面板顯示 14 張照片，圖檔確實是從 `west-usa-2026.pages.dev` 這個網域載入（不是你的電腦）；`robots.txt` 與 `noindex` meta 標籤都正常送出。

**收尾**：GitHub Pages 的設定已停用（避免留一個永遠打不開的 404 網址），對應的 `.github/workflows/deploy-pages.yml` 也刪掉了。**GitHub repo 仍然是程式碼與提交歷史的唯一來源**——之後任何修改都是先 commit 到 GitHub，網站內容更新則是重新跑一次 `wrangler pages deploy`（這步我還沒寫成腳本，票 08 交付操作說明時會補上）。

---

## 驗證現況

```bash
powershell -ExecutionPolicy Bypass -File scripts\verify.ps1
```

最後一次執行結果：**通過 28、失敗 0**。全綠。

冪等性已手動驗證：連跑兩次 `build-photos.ps1`，第二次「新產生 0、略過 108」，符合票 04 的要求。

---

## 施工中做出的決定（尚未回寫 SPEC.md）

這幾項是實作時才浮現的問題，我當場決定了，**你如果不同意可以推翻**：

1. **資料檔用 `.js` 而非 `.json`**
   原因：`fetch()` 在 `file://` 下會被 CORS 擋掉，而「雙擊 `index.html` 就能看」是硬需求。
   檔案內容是 `window.TRIP_POINTS = { ... };`，手動編輯的體驗和 JSON 幾乎一樣。
   影響：SPEC 與票裡寫的 `points.json` / `photos.json` / `route.json` 實際上是 `.js`。

2. **縮放控制項移到左下角**
   原因：Leaflet 預設放左上角，會壓到網站標題。
   左上角給標題、右上角留給底圖切換鈕（票 07）、左下角給縮放。

3. **地名標籤畫在標記「底下」，且不攔截點擊**
   原因：美西全景下 10 個中文標籤必然互相重疊。原本標籤蓋住了 ③⑤⑦ 三個標記，點不到。
   重疊的標籤只是不好看，被蓋住的標記是壞掉的，所以讓標籤退到後面。

4. **新增 `scripts/serve.ps1` 本機預覽伺服器**
   網站本身不需要伺服器，這支只是開發時驗收互動行為用的。不影響部署。

5. **所有 `.ps1` 必須存成 UTF-8 with BOM**
   原因：PowerShell 5.1 預設用 ANSI 讀 `.ps1`，中文註解會被吃掉導致字串沒收尾、整份腳本解析失敗。
   **之後任何人新增或編輯 PowerShell 腳本都要注意這件事。**

6. **OSRM 路線改用 `overview=simplified` 而非 `overview=full`**
   原因：`full` 模式對這趟 1,700 英里的路線回傳 5.2 萬個座標點，檔案接近 1MB，會拖慢 Leaflet 折線渲染。`simplified` 用 Douglas-Peucker 演算法壓到 124 個點、5.4KB，視覺上看不出差異。

7. **`build-route.ps1` 有個 PowerShell 陷阱，已修正並留了註解**
   `$geoCoords | ForEach-Object { @($lat, $lon) }` 這種寫法在管線中會被自動攤平，巢狀陣列變成一串獨立數字，導致每個座標對被拆成 `[lat,]` 和 `[lon,]` 兩行、JSON 直接壞掉。改成直接在 `ForEach-Object` 裡組字串輸出，不經過中介陣列。**這是 PowerShell 通用的坑，之後寫類似的資料轉換腳本要注意。**

8. **路線的已知落差已修正，不再是「已接受的簡化」**
   使用者指出馬蹄灣/羚羊谷後實際是折返回大峽谷才去拉斯維加斯。加了一個不顯示標記的途經點「大峽谷（返程）」解決。里程因此從 1,729.7 英里變成 1,862.5 英里（比 Google 截圖的 1,704 多約 9%，因為折返段本來就要多開）。SPEC.md 已同步更新。

9. **照片管線加了 EXIF 方向修正，這是原本票 04 沒明講但必要的一步**
   實測 108 張裡有 19 張帶 EXIF 旋轉標記（相機直拍產生）。`System.Drawing` 不會自動轉正，不修正的話這 19 張會整個躺平顯示。已加入標準的 8 種 EXIF 方向對照表，修正後的尺寸（寬高可能對調）才是寫進 `photos.js` 的「原始尺寸」。已挑一張目視確認轉正成功。

10. **照片壓縮的冪等性用來源檔 mtime 比對，而非重新計算雜湊**
    比對縮圖/大圖的最後修改時間是否早於來源檔。這表示如果使用者手動去動過某張輸出檔（不太可能但理論上），腳本會誤判為已是最新而跳過。目前沒有處理這個邊界情況，因為輸出檔向來不該被手動編輯。

11. **面板寬度用 `min(480px, 92vw)`，不是固定 480px**
    桌機上跟固定 480px 沒有肉眼可見的差異，但視窗變窄時會自動跟著縮，不會把地圖擠到消失。這是為了不讓票 05 的程式碼變成日後補手機版時的絆腳石。

12. **照片牆欄數用 CSS Grid 的 `auto-fill` + `minmax`，不是媒體查詢**
    這樣欄數會隨面板實際寬度連續變化，不會卡在固定的斷點上跳格。是本來就存在、不需要額外解釋的標準做法，這裡記錄是因為它剛好也符合「不要寫死尺寸」的要求。

13. **燈箱翻到頭尾直接繞回另一端，不是停住或把按鈕變灰**
    第一張往前翻繞到最後一張、最後一張往後翻繞回第一張。票裡只要求「行為明確且不出錯」，沒指定要繞回還是卡住；繞回是燈箱類元件最常見的做法，也不用處理「按鈕該不該變灰」這種額外的視覺狀態。

14. **燈箱關閉暗色背景的判斷用 `e.target === lightboxEl`**
    點擊事件會冒泡，但 `e.target` 永遠是使用者實際點到的最內層元素。只有真的點在暗色背景本身（不是圖片、不是按鈕）時 `e.target` 才會等於燈箱容器自己，不需要在圖片或按鈕上額外呼叫 `stopPropagation()`。

15. **底圖切換鈕做在標題區塊裡（左上角），不是慣例的右上角**
    票裡只說「畫面角落」，沒指定哪一角。右上角是地圖 UI 的慣例位置，但這個網站的面板是從右側整個滑出、寬度可達視窗的 92%，常駐控制項放右上角在面板打開時會被整個蓋住，違反票裡「不與面板重疊」的要求。左上角（跟標題同一塊）是唯一保證不會被面板蓋住的角落。

16. **修正了加切換鈕帶出來的標記遮擋回歸**
    標題區塊變高後，原本對稱的 `fitBounds({padding:[70,70]})` 讓 Napa Valley 的標記被蓋在標題底下。改用 Leaflet 的 `paddingTopLeft`/`paddingBottomRight` 做非對稱留白（左上角 180×170、右下角維持 70×70），讓地圖自動視野主動避開左上角這塊常駐 UI。**這只保證「初始視野」不重疊**——使用者之後手動拖曳地圖，理論上還是可能把某個標記拖到標題底下，這跟其他地圖類 App（Google Maps 等）的行為一致，沒有特別處理，也不打算處理。

---

## 尚未解決 / 需要你決定

1. **全景下的標籤擁擠**
   10 個中文地名在初始視野下會互相重疊（見票 01 的驗收截圖）。目前的處置是「標記永遠在上、標籤可以被壓住」，功能上沒問題，但視覺上有點亂。
   可選的改法：低縮放層級時只顯示編號、放大後才顯示地名。這會改變第一眼的觀感，所以我沒有自作主張。

2. **`points.js` 裡所有 `date` 欄位都是空的**
   我沒有可靠依據判斷每個地點是哪一天，**刻意不編造**。你之後可以自己填，格式自由。填了面板就會顯示，不填就不顯示。

3. **票 02（部署）需要你參與**
   要推上 GitHub 需要帳號驗證（token 或 SSH），這一步我不能代你做。而且推上去等於**把照片公開到網路上**，我會在動作前先跟你確認。

4. ~~票 05、票 06 的瀏覽器驗收都是在很小的預覽視窗下做的~~ **已在票 08 用 DOM 層級精確斷言補齊**（見上面「票 08：完整驗收與上線」章節）。結論：不是網站的 bug，是沙盒瀏覽器工具在指定 1920×1080 這類尺寸時自己的限制，已查證清楚原因。

5. **面板／燈箱疊層在截圖上「看起來」對不對，我沒辦法自己確認**
   DOM 屬性層級（背景色、透明度、z-index、圖片載入狀態）全部檢查過都是正確值，但沙盒工具的截圖在這種疊層效果上似乎不太可靠，畫面看起來的樣子跟 DOM 狀態對不上。**建議你自己在瀏覽器上點開一次面板和燈箱看一眼**，理論上沒問題，但這是全專案中少數幾個我沒辦法用截圖親眼證實、只能靠程式狀態間接證明的地方。

6. **Standards code review 抓到的問題（尚未處理，等你確認要不要修）**
   跑了 `/mp-code-review`（跟空 repo 比較，涵蓋全部 8 張票的變更），Standards 軸的審查發現：
   - **`Read-DataFile` 這段解析 `.js` 資料檔的邏輯，在 `build-photos.ps1`、`build-route.ps1`、`verify.ps1` 三支腳本裡逐字複製了三份**。日後如果資料檔的格式（比如註解語法）要改，得記得同步改三個地方。可以抽成一支共用的 `scripts/lib/DataFile.ps1` 讓三支腳本用 dot-source 引入。
   - **`verify.ps1` 裡把「照片總張數必須恰好 108」寫死當斷言**，這跟整個專案「照片數量會持續增加」的核心精神矛盾——你下次補照片、張數變成 109 之後，這條檢查就會直接報錯，即使一切正常。這個我覺得**該修**，其他兩項屬於「值得知道但不急」的判斷。
   - Spec 軸的審查結果見下方（如果還在跑，稍後補充）。

---

## 環境狀態

- **git**：repo 已初始化，有 13 個 commit，**已推送到 GitHub**。`user.name` / `user.email` 只設在**這個 repo 內**（`liuwe` / `ja604041062@gmail.com`），**沒有動你的全域設定**。
- **遠端**：`origin` = `https://github.com/ja604041062/west-usa-2026.git`，分支 `main`，與本機同步。
- **正式網站**：https://west-usa-2026.pages.dev/（Cloudflare Pages，Direct Upload 部署，不是 Git 整合）。
- **這次部署過程中新裝的工具**（都用 `winget`，都是標準安裝，沒有動系統設定）：
  - **GitHub CLI（`gh`）**——已登入 `ja604041062`。指令列建 repo、push、操作 GitHub API 用。
  - **Node.js**——單純是因為 Cloudflare 的 `wrangler` 這個部署工具需要 npm 才能執行，這台電腦上目前沒有其他用途在用它。
  - `wrangler`（透過 `npx` 執行，沒有全域安裝）——已登入 Cloudflare `Ja604041062@gmail.com`。之後要重新部署網站內容，就是重跑「複製檔案到乾淨資料夾 + `wrangler pages deploy`」這個流程（票 08 會把這個寫成腳本）。
- **預覽伺服器**：可能還在背景執行（port 8765）。要停止就關掉那個 PowerShell 視窗，或重開機。重新啟動的指令見下。

---

## 下次從這裡繼續

下一步是**票 08：完整驗收與上線**。網站已經上線了，這張票剩下的是把之前欠的驗收補齊：

1. **補做票 05、票 06 欠的正式桌機尺寸視覺驗收**（1920×1080、1440×900）——之前受限於沙盒瀏覽器工具的視窗大小，只做了程式狀態層級的驗證，沒有用眼睛在正式尺寸下看過一輪
2. **跑一輪完整的人工驗收清單**（SPEC.md 裡「人工驗收清單」那節列的項目）——建議你在自己的瀏覽器上、對著 https://west-usa-2026.pages.dev/ 實際操作一次
3. **把「重新部署」這個手動流程寫成腳本**（例如 `scripts/deploy.ps1`），現在是我手動下的幾個指令，應該收斂成你自己也能重跑的一個腳本
4. **交付一份操作說明**給你——怎麼補照片重跑、怎麼加新地點（含把途經點升級成正式停留點）、怎麼填遊記文字、怎麼重新部署上線。這是你要能獨立維護這個網站的關鍵，沒有這份說明，前面做的「可重跑設計」等於白做
5. 確認 `.wrangler/` 本機快取沒有被誤傳到公開網站（已在 `.gitignore` 排除，但沒有在部署腳本裡明確驗證過）

**啟動預覽伺服器**：

```bash
powershell -ExecutionPolicy Bypass -File scripts/serve.ps1
```

**跑驗證**：

```bash
powershell -ExecutionPolicy Bypass -File scripts/verify.ps1
```
